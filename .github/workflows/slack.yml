  name: slack
  on: [push]
  jobs:
   slack:
    runs-on: ubuntu-latest

    steps:
      - name: Notify slack
        env:
          LACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
        uses: pullreminders/slack-action@master
        with:
          args: '{\"channel\":\"C07554HPTHT\",\"text\":\"Hello world\"}'
